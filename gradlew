#!/bin/sh
# gradlew script (minimal version)
APP_NAME="Gradle"
APP_BASE_NAME=`basename "$0"`
exec sh -c "echo 'This is a placeholder for gradlew. In GitHub Actions, it will be replaced or executed by the actual Gradle wrapper.'"
